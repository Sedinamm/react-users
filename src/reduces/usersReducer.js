const initialState = {
    users: [
        {name: "Sedinam",
        email: "sedinam436@gmail.com",
        gen: "19"    
    },
    ]
};

const userReducer = (state =initialState, action) =>{
    return state;
};

export default userReducer;